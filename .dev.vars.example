# Local development secrets. Copy to `.dev.vars` and fill in.
# The real production values are set via `wrangler secret put` (not in this file).
# .dev.vars is gitignored and only used by `wrangler dev` locally.

ADMIN_TOKEN=replace-with-a-long-random-string-only-glenn-knows
