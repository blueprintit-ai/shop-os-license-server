{
  "name": "shop-os-license-server",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that issues, validates, and revokes Shop OS license keys.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check:sync": "node scripts/check-asset-sync.mjs",
    "predeploy": "node scripts/check-asset-sync.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "@cloudflare/vitest-pool-workers": "^0.16.9",
    "@cloudflare/workers-types": "^4.20260526.1",
    "typescript": "^5.3.0",
    "vitest": "^4.1.7",
    "wrangler": "^4.95.0"
  }
}
